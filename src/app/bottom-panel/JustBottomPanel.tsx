import {JustId} from "@/app/components/just-layout/justLayout.types.ts";

interface Props {
  justId: JustId
  layoutId: string
}

function JustBottomPanel({justId: _justId, layoutId: _layoutId}: Props) {
  return (
    <div></div>
  )
}

export default JustBottomPanel